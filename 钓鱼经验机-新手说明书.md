# 钓鱼经验机 新手说明书

> 适用于单文件版本 `钓鱼经验机.exe`（v0.1.3），**不需要安装 Python，不需要敲任何命令行**，双击就能用。
> 本说明书面向从没用过命令行、不懂编程的普通玩家。

## 一、这是什么，能帮你做什么

「钓鱼经验机」是一个开源的《三角洲行动》"台竿钓鱼"自动化小工具。它会不断截取你游戏窗口的画面，用 OpenCV 判断水面有没有出现咬钩水花，然后模拟鼠标自动完成**抛竿、提竿、换饵**这套重复操作，帮你省下一直盯着屏幕的力气。

它的几个前提你要先知道：

- **只支持 Windows x64 系统。** 程序直接调用 Windows 的鼠标接口来模拟点击，别的系统用不了。
- **必须用管理员权限运行。** 因为游戏通常也是以管理员权限运行，权限不对程序就点不动游戏。双击后会弹出 UAC 授权窗口，必须点「是」。
- **不需要独立显卡、不需要 CUDA、不需要任何视觉模型**，纯 CPU 就能跑，识别过程全部在你本机完成，不上传云端。
- 建议给它预留 **1~2 GiB 内存、1 GiB 磁盘空间**。
- 它是**本地自动化辅助工具，不含任何反作弊绕过功能**。

另外提前说一句最重要的：**程序里的"提竿信号"不等于你成功获鱼的数量。** 它只是按画面亮度判断"像是水花"，不等于游戏一定判定你钓上了鱼。参数来自有限实测，不保证适配所有场景。

---

## 二、一分钟快速上手

如果你只想先跑起来看看效果，照下面做（第 3 步的框选是全部关键，别跳过）：

1. 把 `钓鱼经验机.exe` 放到**桌面**或 D 盘一个普通文件夹里，双击运行，UAC 弹窗点「是」。
2. **先开游戏**，再回到程序，在列表里选中**游戏窗口**；看不到就点「刷新窗口」。
3. 模式先选 **「识别预览（不点击）」**——这个模式**完全不点鼠标**，随便试，绝对安全。
4. 点「框选水花区域」，然后**在 3 秒内切回游戏并按住右键放大**，程序会截一张静态图，你在图上**拖动鼠标框选水花会出现的区域**，**松开左键保存**（按 Esc 取消）。
5. 点「框选鱼饵数字」，同样 **3 秒内切回游戏**，然后**只框住最后一位数字（0~5）**——例如鱼饵显示 `005 / ∞` 时，只框那个 `5`，不要带前面的 `00`，也不要带 `/∞`。
6. 勾选「显示识别到的亮色区域」看一眼：水面平静时亮色占比应该**低于**"平静面积上限"，出现咬钩水花时应该**高于**"水花触发面积"。正常了再往下。
7. 勾选「显示鱼饵裁剪」，确认鱼饵数字读对了。
8. **收回鱼竿**，把模式切成 **「自动循环」**，点「开始」，然后 **5 秒内切回游戏窗口**。
9. 需要停下来时：**F8 开始/暂停，F9 立即停止**。

> 第一次用，强烈建议前 10 分钟都留在「识别预览（不点击）」模式里，把框选和阈值调稳，再切到「自动循环」。

---

## 三、运行前的准备

| 项目 | 要求 |
| --- | --- |
| 操作系统 | 仅 Windows x64（源码直接使用 Windows API 模拟鼠标） |
| 权限 | 需要**管理员权限**，双击后 UAC 弹窗必须点「是」 |
| 显卡 | 不需要独立显卡，不需要 CUDA，不需要视觉模型，纯 CPU 运行 |
| 内存 | 建议预留 1~2 GiB |
| 磁盘 | 建议预留 1 GiB |
| 网络 | 识别不上云；程序不会自动上传你的任何数据 |

**关于首次启动慢**：这是**单文件 exe 的正常现象**。单文件程序第一次运行时需要先把自身解压到临时目录，所以会比普通程序慢几秒，耐心等一下就好，不要连点好几次。

---

## 四、程序把文件放在哪里（很重要）

打包成 exe 之后，**所有配置和日志都保存在 exe 所在的那个文件夹里**，不会写进系统目录，也不会自动上传到任何地方。

| 文件 / 文件夹 | 里面是什么 | 你要注意什么 |
| --- | --- | --- |
| `local_config.json` | 你调好的所有参数 | **换电脑或换文件夹时，把这个文件一起带走，设置就不会丢** |
| `logs\` | 运行日志和鱼饵裁剪截图 | 反馈问题时，可以把**脱敏后**的日志片段附上 |

**如果 exe 放在只读位置**（例如某些受保护的目录、只读 U 盘），程序会退而把配置写到：

```
%LOCALAPPDATA%\FishingAssistant\
```

所以给普通用户的建议是：**直接把 exe 放在桌面，或者 D 盘一个普通文件夹里**，别放在只读的地方。

---

## 五、完整使用流程（一步一步来）

### 第 1 步：启动程序

把 `钓鱼经验机.exe` 放进一个普通文件夹，双击。弹出 UAC 授权窗口时**必须点「是」**——不给管理员权限，程序就没法向游戏发送鼠标操作，后面点了也没反应。

### 第 2 步：选中游戏窗口

界面上先选**游戏窗口**。如果列表里没有你想要的窗口，点「刷新窗口」。

> 注意：**游戏要先开着**，程序才能找到它的窗口。

### 第 3 步：先进入「识别预览（不点击）」模式

这个模式**完全不点鼠标**，只做识别和显示，可以放心试。所有框选、调参、验证都建议在这个模式里做完。

### 第 4 步：框选水花区域

1. 点「框选水花区域」。
2. **在 3 秒内切回游戏，并按住右键放大**（保持和你实际钓鱼时一样的放大视角）。
3. 程序会截取一张静态图，你在图上**拖动鼠标框选**水花会出现的那块区域。
4. **松开左键保存**；不想框了按 **Esc 取消**。

要点：框选要覆盖水花实际出现的范围，同时尽量避开水面的高光倒影、泡沫、无关的浪尖。

### 第 5 步：框选鱼饵数字

1. 点「框选鱼饵数字」。
2. 同样**在 3 秒内切回游戏**。
3. **只框选最后一位数字（0~5）**。例如鱼饵显示 `005 / ∞` 时，只框那个 `5`，**不要带前面的 `00`，也不要带 `/∞`**。
4. 框的宽度要**能装下最宽的数字**，不要按 `1` 的宽度框死，否则数字变成别的形状时就读不出来了。

### 第 6 步：检查识别效果（亮色区域）

勾选「显示识别到的亮色区域」，然后观察：

| 画面状态 | 期望结果 |
| --- | --- |
| 水面平静时 | 亮色占比**低于**"平静面积上限" |
| 咬钩水花出现时 | 亮色占比**高于**"水花触发面积" |

如果**整片水面都被标记了**，说明框选位置或范围不对。**先换钓点或重新框选，再考虑调参数。**

### 第 7 步：检查鱼饵识别

勾选「显示鱼饵裁剪」，确认数字读对了。下面三种都要能读对：

- 白色的正常鱼饵数字；
- 红色的低饵量数字；
- 换饵之后的 `5`。

### 第 8 步：切换到「自动循环」正式开始

1. 确认识别都正常后，**把鱼竿收回来**。
2. 把模式切换成 **「自动循环」**。
3. 点「开始」。
4. **在 5 秒内切回游戏窗口**。

### 第 9 步：快捷键与自动保护

- **F8：开始 / 暂停。**
- **F9：立即停止。**
- 如果热键和你正在用的其他软件冲突，**界面底部会显示可用的备用组合键**。
- 当程序检测到**你切出了游戏、程序出现异常、或者等待超时**时，它会**自动暂停**，并且会**松开它之前按住的鼠标键**——不会把鼠标键一直卡着。

---

## 六、调参原则（新手最容易在这里乱调）

先记住这一条总原则：

> **先暂停，每次只改一个参数，点「保存参数」，再重新开始预览。运行中改参数不会立刻生效。**

再理解一下程序到底在看什么：

> 程序判断水花的方式是：**"够亮、颜色够淡的像素"占你框选面积的百分比**。它**不是**图像识别模型，不认识"鱼"，只认识"亮色块的占比"。

所以：

- 界面里填 `0.5` 就表示 `0.5%`。
- 面积占比是**以你框选的区域为分母**算的——**改变框选大小之后，必须重新检查阈值**。

### 6.1 关键参数与默认值

| 参数 | 默认值 | 说明 |
| --- | --- | --- |
| 亮度下限 | 80 | 低于它的像素不算亮 |
| 饱和度上限 | 100 | 高于它的像素不算"颜色够淡" |
| 水花触发面积 | 约 0.8% | 亮色占比超过它，判定为水花、提竿 |
| 平静面积上限 | 约 0.4% | 亮色占比低于它，判定为水面平静 |
| 普通提竿后等待 | 2.8 秒 | 收鱼动画等它结束 |
| 提竿加换饵后等待 | 4.8 秒 | 换饵后多等一会儿 |
| 抛竿后屏蔽 | 3.5 秒 | 刚入水的水花不参与判断 |
| 抛竿后右键 | 0.7 秒 | 抛竿后延迟多久按右键放大 |
| 平静确认 | 0.2 秒 | 平静状态需要连续确认的时长 |
| 检测频率 | 30 FPS | 每秒判断画面的次数 |
| 等待咬钩超时 | 60 秒 | 超过就进入保护性暂停 |

> **硬性规则：平静面积上限必须小于水花触发面积。** 否则程序会直接逻辑报错。最常见的"参数非法"提示，就是这一条被违反了。

### 6.2 常见情况怎么调

| 你遇到的情况 | 怎么调 |
| --- | --- |
| 水面很暗，有水花但不提竿 | 先把亮度下限 `80 → 70 → 60`；如果水花已经被标记但占比不够，再把触发面积 `0.8 → 0.6 → 0.5` |
| 反光明显 | 优先换朝向或换钓点，框选时避开高光倒影；若反光比水花暗，亮度下限试 `80 → 100 → 120`；若只剩小块反光误触发，触发面积试 `0.8 → 1.0 → 1.2` |
| 大波纹、亮色浪尖误触发 | 缩小框选排除无关浪尖（但仍要覆盖完整水花）；把触发面积逐步提高 `0.8 → 1.0 → 1.2`，同时确认真正的咬钩水花还能超过它 |
| 一直显示"等待平静" | 先排除反光、泡沫和框选干扰；若普通水面占比确实偏高，可适当提高平静面积上限，但它应**高于平静水面占比、低于触发面积** |
| 收鱼动画没结束就抛竿 | 把 `2.8` 秒每次加 `0.2~0.5` 秒 |
| 最后一饵后抛得太早 | 把 `4.8` 秒每次加 `0.2~0.5` 秒，同时检查换饵后的 `5` 能不能识别 |
| 入水水花没散就误提竿 | 抛竿后屏蔽 `3.5 → 4.0` 秒 |
| 抛竿后右键放大没生效 | 抛竿后右键 `0.7` 秒每次加 `0.1~0.2` 秒；改完要**重新检查放大视角下的区域** |

**每次只改一个参数**，改完点「保存参数」，回到「识别预览（不点击）」看效果，确认没问题再切「自动循环」。

> 补充：如果实机调不稳，程序还带一个**离线录像验证**功能（界面右侧「录像验证」标签页）。
> 你可以录一段实际钓鱼的视频，填入每次甩竿的起始秒数后点「离线回放」，
> 让程序在**不进游戏、不点鼠标**的情况下跑一遍识别，专门用来确定阈值。
> 录像里的观测结果只代表那一段样本，不等于真实对局的成功率。

---

## 七、常见问题排查

| 现象 | 怎么办 |
| --- | --- |
| 双击没反应 / UAC 弹窗被拒绝 | **必须允许管理员权限**，否则无法向游戏发送鼠标操作 |
| 启动要等好几秒 | 单文件 exe 的正常现象，稍等即可 |
| 窗口列表里找不到游戏 | 先开游戏，再点「刷新窗口」 |
| 一直"等待平静"或亮色超标 | 按第 6 章检查框选区域、亮度下限、平静面积上限 |
| 有水花但不提竿 | 确认当前是「自动循环」模式、入水屏蔽时间已结束并进入了检测状态；再按"暗水面"的步骤检查标记和占比 |
| 点击无反应 | 用界面上的「单次左键测试」排查（它只点一次）；检查所选窗口和运行权限 |
| 运行时失去游戏焦点 | 必须在倒计时内切回所选游戏窗口，运行过程中不要切出去 |
| 提示参数非法 | 最常见是"平静面积上限 ≥ 水花触发面积"，把平静上限改小或把触发面积加大 |
| 提示缺模块 | 只有**从源码运行**才会遇到，exe 版不会。在项目目录执行 `.\.venv\Scripts\python.exe -m pip install -r requirements.txt` |

---

## 八、合规提醒（请务必阅读）

- 本项目代码按 **MIT 协议**开源；**游戏名称、截图和画面素材的权利属于各自权利人**。
- 本项目使用**普通屏幕截图和 Windows 输入接口**，**不含反作弊绕过功能**。
- **是否允许在具体游戏或服务器中使用自动化，应以游戏运营方的规则为准；代码开源不代表游戏运营方授权。**
- 请自行判断使用风险。

---

## 九、给想自己打包的人

如果你想自己生成这个单文件 exe：

- 打包**必须在一台 Windows 上执行**。
- 仓库里已经提供了 **PyInstaller 打包配置**和 **GitHub Actions 自动构建流水线**：把代码推送上去后，可以在仓库的 **Actions 页面下载自动生成的 exe**。
- 想在本机打包，运行仓库里的 **`build_exe.bat`** 即可。

---

## 十、从源码运行的开发者（普通用户可跳过）

如果你不是用 exe，而是直接从源码运行，才会遇到"缺模块"之类的提示，此时在项目目录里安装依赖即可：

```
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
```

普通玩家用 exe 版本，**完全不需要 Python、pip、PowerShell 或任何命令行操作**。
